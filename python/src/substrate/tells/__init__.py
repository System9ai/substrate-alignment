"""Behavioral-tell detection.

Pure-logic primitives that surface behavioural tells: observable
patterns that *inform* trust scoring and reasoning-mode interpretation
without producing standalone judgements. The discipline is that tells
are substrate-state evidence feeding downstream interpretation, never
direct verdicts: cultural variation, neurodivergence, and current
health state all produce tells that look superficially like
substrate-misalignment, so the primitives surface the signals and
leave the inference to the host application.
"""

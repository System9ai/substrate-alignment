spec: runaway-power-prevention
spec_version: ">=0.2"
scenario: mech-4__band-zone-warning
required: true
metadata:
  mechanism: 4
  rationale: >
    Layered zones. (1/φ, 2/3] is the WARNING band, winded, the
    approach to burnout, the mirror of the calibration band. It is NOT
    yet DEBT: the uniform debt line is 2/3. 0.65 MUST classify as
    warning, never debt (the obsolete debt@0.618 model is an inversion).

input:
  mechanism: resistance-band
  utilization: 0.65

expected:
  zone: warning

spec: runaway-power-prevention
spec_version: ">=0.1"
scenario: mech-4__band-under-loaded
required: true
metadata:
  mechanism: 4
  rationale: >
    Mechanism 4, ResistanceBand. A utilisation below the default lower
    bound (1/3) MUST classify as UNDER_LOADED.

input:
  mechanism: resistance-band
  utilization: 0.20

expected:
  classification: under_loaded
